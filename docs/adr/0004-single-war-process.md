# ADR-0004：一个进程只运行一个 WAR

## 状态

Accepted

## 背景

目标项目本身按一个容器一个应用部署；多 Host、多 Context 会引入复杂映射、隔离和热部署问题。

## 决策

每个 tinysc 进程只加载一个 WAR，但允许显式配置 context path。

## 后果

正面：生命周期、故障域、资源配额和滚动升级更清晰。负面：多个应用需要多个进程。

## 备选方案

复制 Tomcat 多层容器树：拒绝，首版没有对应需求且显著增加复杂度。
