# ADR-0005：事务式部署与原子发布

## 状态

Accepted

## 背景

部分初始化的 WebApp 若接收流量，会产生不确定行为和资源泄漏。

## 决策

WAR 在 staging 中完成检查、类加载和组件初始化；全部成功后一次性发布为 ActiveApplication。
失败时逆序销毁并关闭所有资源。

## 后果

正面：请求只看到完整可用应用。负面：启动阶段需要额外 staging 空间和明确回滚逻辑。

## 备选方案

边扫描边开放流量：拒绝，因为无法定义一致的可用状态。
